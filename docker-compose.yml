version: '3'

services:
  postgres:
    image: 'postgres:12'
    environment:
      POSTGRES_USER: 'postgres'
      POSTGRES_PASSWORD: ''
      POSTGRES_DB: 'sandbox'
    ports:
      - '5433:5432'
    volumes:
      - database:/var/lib/postgresql/data
volumes:
  database:
